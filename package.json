{
  "name": "token-consumption-leaderboard",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter token-leaderboard dev",
    "dev:local": "pnpm --filter token-leaderboard dev",
    "build": "pnpm --filter token-leaderboard build",
    "test": "playwright test",
    "test:local": "playwright test --project=local-app"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
