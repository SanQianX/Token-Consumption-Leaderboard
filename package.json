{
  "name": "token-consumption-leaderboard",
  "private": true,
  "version": "2.0.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter tokmeter dev",
    "build": "pnpm --filter tokmeter build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
