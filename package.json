{
  "name": "token-consumption-leaderboard",
  "private": true,
  "version": "2.0.1",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter token-leaderboard dev",
    "build": "pnpm --filter token-leaderboard build",
    "check": "pnpm --filter token-leaderboard build:all && node scripts/check-package.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
